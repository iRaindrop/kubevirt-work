---
mode: agent
description: Write a comment on the inclusive language of the documentation.
---

Run the shared **comment** analysis for the `inclusive-language` criteria area.

1. Read the criteria definition in `.github/prompts/criteria/inclusive-language.md`.
2. Read and follow the shared procedure in `.github/prompts/comment.prompt.md`, treating `inclusive-language` as the selected area.

Optional output filename override: `${input:title}` — if non-empty, use it as the output filename; otherwise use the engine's default (`<project-slug>-<stem>-comment.md`).
